# -*- mode: python -*-

Import('env')

env.SConscript(dirs=[
    'db',
    'net'
])

env.Program('server',
    source=['main.cc'],
    LIBS=['mongoose', 'jsoncpp', 'net', 'db'],
    LIBPATH=['#/lib/mongoose', '#/lib/jsoncpp/src/lib_json', 'net', 'db'])
